fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.2.0
websockets>=11.0
python-multipart>=0.0.6
matplotlib>=3.7.0
seaborn>=0.12.0
xgboost>=1.7.0
lightgbm>=4.0.0
shap>=0.42.0

# Note: Windows/CPU users may need to follow PyTorch's official install selector at pytorch.org instead of a plain pip install if the default wheel does not match their system environment.
torch>=2.0.0
torch-geometric>=2.3.0
